{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for Dynasty Wire",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "store:default"
  ]
}
